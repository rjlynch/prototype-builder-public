<%# Checkboxes share their editor fields with radios; unlike radios they have
    no inline option (the GOV.UK checkboxes component is always stacked). %>
<%= render "components/editors/choice_fields", f: f %>
